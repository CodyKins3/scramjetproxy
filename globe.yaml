name: scramjet-proxy
entrypoint: bin/main.dart

# optional, but nice to include
env:
  PORT: "8080"

# what files should be included when deploying
include:
  - pubspec.yaml
  - bin/**
  - lib/**
  - globe.yaml
